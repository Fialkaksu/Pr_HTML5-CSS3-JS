// Task #2
// Write a function - stringToNumber
// It should accept one argument as a string and return it as a number
// Tip: Don’t worry about incoming string – it’s always valid

function stringToNumber(str) {
	return Number(str);
}
stringToNumber('1258');